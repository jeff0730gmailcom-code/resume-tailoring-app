fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
openai==2.46.0
# Already an indirect dependency of openai's async client (used for its
# request transport) - depended on directly by app/services/ai_tailor.py
# to build a fine-grained connect-vs-read httpx.Timeout for OpenAI calls.
httpx>=0.27,<1

# CV text extraction
pypdf==6.14.2
python-docx==1.2.0

# SQLite database (resume templates + resume-generation history) - see
# app/db/session.py, app/db/models.py.
SQLAlchemy==2.0.51

# Resume template rendering: Jinja2 fills each app/templates/resumes/<slug>/
# template.html.jinja2 with the tailored content; Playwright's headless
# Chromium renders that HTML to PDF (see app/services/template_renderer.py).
# One-time setup step after installing this file:
#   playwright install chromium
Jinja2==3.1.6
playwright==1.62.0

# Rasterizes each template's reference.pdf into its gallery thumbnail PNG at
# startup (see app/services/template_registry.py) - no browser involved.
pymupdf==1.28.0

# Windows-only: drives Microsoft Word via COM to convert a rendered resume
# PDF into an editable .docx download (see app/services/docx_to_pdf.py).
# Requires Microsoft Word to be installed on the host machine.
pywin32==312; sys_platform == "win32"
